# Railway Deployment Configuration
# This file tells Railway how to deploy your flight plan listener

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
